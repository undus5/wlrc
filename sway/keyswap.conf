# read `man 7 xkeyboard-config` for a complete reference.
# caps as ctrl, swap alt and win, enable numlock
input type:keyboard {
    xkb_options 'ctrl:nocaps,altwin:swap_alt_win'
    xkb_numlock enabled
}
